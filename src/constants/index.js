import icons from './icons';
import images from './images';
import { COLORS, SIZES, FONTS } from './theme';
import GOOGLE_API_KEY from './maps';

const IS_TRANSIT = 'is_transit';
const IS_TRIP = 'is_trip';

export {
  icons,
  images,
  COLORS,
  SIZES,
  FONTS,
  GOOGLE_API_KEY,
  IS_TRANSIT,
  IS_TRIP,
};
